# bash
Bash scripts 

Password Generator:
Creates 3 passwords with base64, encrypts them using ccrypt and then saves in a file. Voila!!

System Performance:

- Displays current CPU usage percentage
- Calculates available memory as a percentage
- Shows total disk usage in human-readable format
- Lists top processes consuming the most CPU
- Lists top processes consuming the most memory
- Uses standard Linux system tools 
- Outputs a quick system resource summary in a single run
- Simple modular function-based structure for readability
- Easily extendable for additional system metrics
